apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-srv
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
      http:
        paths:
          - path: /posts
            backend:
              serviceName: posts-svc
              servicePort: 4000
